import { Errors, ErrorsList } from './types';
export declare const ErrorMessages: Errors;
export declare class MediaWikiJSError extends Error {
    code: string;
    constructor(key: ErrorsList, ...args: unknown[]);
    get name(): string;
}
